{
  "name": "staff-monitoring-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix server start",
    "server": "npm --prefix server run dev",
    "dev": "npm --prefix client run dev -- --host 0.0.0.0"
  }
}
